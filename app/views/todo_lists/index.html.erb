<h1>New To-Do list</h1>
<%= link_to "New Todo List", new_todo_list_path%>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @todo_lists.each do |tl|%>
      <tr>
        <td><%= tl.name %></td>
        <td><%= tl.description %></td>
        <td>
          <%= link_to "Show", todo_list_path(tl)%>
          <%= link_to "Edit", edit_todo_list_path(tl)%>
          <%= link_to "Delete",todo_list_path(todo_list),data: {turbo_method: :delete,turbo_confirm: "Are you sure?"} %
        </td>
      </tr>
      <% end %>
  </tbody>
</table>